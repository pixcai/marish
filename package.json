{
  "name": "marish",
  "version": "1.0.0",
  "description": "API mock and proxy platform",
  "main": "src/index.js",
  "scripts": {
    "dev": "cross-env NODE_ENV=development node --use-strict -r dotenv/config src/index.js dotenv_config_debug=true",
    "start": "cross-env NODE_ENV=production node --use-strict -r dotenv/config src/index.js",
    "lint": "standard --fix"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/pixcai/marish.git"
  },
  "license": "MIT",
  "dependencies": {
    "cross-env": "^5.2.0",
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "express-react-views": "^0.10.5",
    "mongoose": "^5.3.8",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0"
  },
  "devDependencies": {
    "husky": "^1.1.2",
    "standard": "^12.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint"
    }
  },
  "standard": {
    "ignore": [
      "**/public/"
    ]
  }
}
